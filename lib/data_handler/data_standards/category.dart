enum CompositeDataCategory {
  profit,
  shoppingMall,
  gender,
  price,
  age,
  sellingTime
}

enum TotalDataCategory {
  totalSummary,
  trend,
  profitLoss,
  financialTable,
  expectedProfit
}

enum ViewType { ratio, trend, treemap, column, calendar, schedule, table }

enum SubChartType { total, age, mall, date }
